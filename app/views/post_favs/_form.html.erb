<%= form_for(@post_fav) do |f| %>
  <% if @post_fav.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_fav.errors.count, "error") %> prohibited this post_fav from being saved:</h2>

      <ul>
      <% @post_fav.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.text_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
